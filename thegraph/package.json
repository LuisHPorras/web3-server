{
  "name": "dbadge",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dbadge",
    "create-local": "graph create --node http://localhost:8020/ dbadge",
    "remove-local": "graph remove --node http://localhost:8020/ dbadge",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dbadge"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
